en:
  site_settings:
    see_tl3_progress_enabled: "Enable this plugin"
    progress_bar_color_light: "Color for the progress bar for light color palettes. Must be a valid CSS color (RGB/HSL/hex/name)"
    progress_bar_background_color_light: "Background color for the progress bar for light color palettes. Must be a valid CSS color (RGB/HSL/hex/name)"
    progress_bar_color_dark: "Color for the progress bar for dark color palettes. Must be a valid CSS color (RGB/HSL/hex/name)"
    progress_bar_background_color_dark: "Background color for the progress bar for dark color palettes. Must be a valid CSS color (RGB/HSL/hex/name)"
    modal_button_icon: "Icon for the button to show the progress"
    show_verbose_tl3_progress: "Show users the detailed breakdown into which requirements they have not hit, and its corresponding values"
    show_locked_at_trust_level: "Indicate that the user is locked at their Trust Level and cannot be promoted. WARNING: This could cause users to be less active or cause moderation appeals"
    modal_bottom_text: "Additional, optional text at the bottom of the modal"
    show_next_closest_stat: "Show the next closest requirement that can be met for users"
    show_warning_when_tl3_requirements_low: "Show a warning on the user summary page if they are close to losing TL3"
    show_warning_when_user_has_muted_topics_and_posts: "Show a warning if the user has topics and posts in a muted category"
    low_tl3_stats_minimum: "Requires 'show_warning_when_tl3_requirements_low' to be enabled. Minimum threshold for low stats: stats below this number would be calculated and the lowest would be shown to the user"
